# 项目启动傻瓜教程

下面只讲 3 件事：启动前端、启动 SpringBoot、启动 FastAPI。  
请开 **3 个 Git Bash 窗口**，每个窗口跑一个服务。

---

## 1. 启动前端（React）

### 步骤

1. 打开终端，进入前端目录：

```bash
cd /d/Github/software_reco/ollama_springboot/ollama-gui-reactjs
```

2. 首次启动先安装依赖（只需要一次）：

```bash
yarn
```

如果你没有 `yarn`，用：

```bash
npm install
```

3. 启动前端：

```bash
yarn start
```

没有 `yarn` 就用：

```bash
npm run start
```

### 成功标志

- 浏览器能打开：`http://127.0.0.1:3000`

---

## 2. 启动 SpringBoot（Java 后端）

### 步骤

1. 打开第二个终端，进入 SpringBoot 目录：

```bash
cd /d/Github/software_reco/ollama_springboot/demo
```

2. 启动服务：

```bash
./mvnw spring-boot:run
```

### 成功标志

- 控制台出现 Spring Boot 启动完成日志
- 服务地址：`http://127.0.0.1:8080`

---

## 3. 启动 FastAPI（Python 后端）

### 步骤

1. 打开第三个终端，进入 FastAPI 目录：

```bash
cd /d/Github/software_reco/fastapi_demo
```

2. 首次启动先安装依赖（只需要一次）：

```bash
python -m pip install -r requirements.txt
```

3. 启动服务：

```bash
conda activate dech2 &&
  python -m uvicorn app.main:app --host 0.0.0.0 --port 8000
```

如果你环境里 `python` 不可用，用：

```bash
python3 -m pip install -r requirements.txt
python3 main.py
```

### 成功标志

- 服务地址：`http://127.0.0.1:8000`
- 健康检查可访问：`http://127.0.0.1:8000/health`

---

## 最后检查（联调是否正常）

三个服务都启动后，应该是：

- 前端：`3000`
- SpringBoot：`8080`
- FastAPI：`8000`

只要这 3 个端口都在线，项目就能跑起来。
